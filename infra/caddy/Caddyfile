# Caddyfile RADAR - reverse proxy + SSL automatique (Let's Encrypt)
# À adapter avec le domaine final.

{
    email contactkaramo@gmail.com
}

radar.example.com {
    encode gzip zstd
    reverse_proxy web:3000
}

agent.radar.example.com {
    encode gzip zstd
    reverse_proxy agent:4000
}
